# VulnForge Academy - Python Backend Environment Variables
# =========================================================
# NOTE: Most of these are OPTIONAL - the app works without them!
# The Python backend uses SQLite by default (no database config needed)

# =============================================================================
# OPTIONAL: Server Configuration
# =============================================================================
# HOST=0.0.0.0
# PORT=8000

# =============================================================================
# OPTIONAL: Security (defaults are built-in for training purposes)
# =============================================================================
# JWT_SECRET_KEY=your-secret-key-here
# JWT_ALGORITHM=HS256
# JWT_EXPIRATION_HOURS=24

# =============================================================================
# OPTIONAL: Database (only if you want to use PostgreSQL instead of SQLite)
# =============================================================================
# DATABASE_URL=postgresql://user:password@localhost:5432/vulnforge

# =============================================================================
# OPTIONAL: CORS (defaults to allow all origins for training)
# =============================================================================
# CORS_ORIGINS=http://localhost:3000,https://vulnforge-academy.vercel.app

# =============================================================================
# OPTIONAL: Frontend URL (for redirects)
# =============================================================================
# FRONTEND_URL=https://vulnforge-academy.vercel.app

# =============================================================================
# NOTE: The following PHP variables are NO LONGER USED
# =============================================================================
# DB_HOST (not used - SQLite is file-based)
# DB_USER (not used)
# DB_PASS (not used)
# DB_NAME (not used)
# SECURITY_ADMIN_EMAIL (not used)
# SECURITY_WEBHOOK_URL (not used)
# RATE_LIMIT_* (not used)
# GITHUB_SPONSORS_URL (not used)
# BUY_ME_COFFEE_URL (not used)
# AFFILIATE_BASE_URL (not used)
